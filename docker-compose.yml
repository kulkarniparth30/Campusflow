services:
  campusflow:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_SUPABASE_URL: 
        VITE_SUPABASE_ANON_KEY: 
        VITE_GROQ_API_KEY: 
    ports:
      - "80:80"
    restart: unless-stopped
